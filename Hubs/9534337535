local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Budgie Hub | Shrek in the Backrooms",
    SubTitle = "by ADSKer",
    TabWidth = 160,
    Size = UDim2.new(0.7, 0, 1, 0),
    Acrylic = false,
    Theme = "Darker",
    MinimizeKey = Enum.KeyCode.LeftControl -- Used when theres no MinimizeKeybind
})

local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "" }),
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/ADSKerOffical/BHU2Project/refs/heads/main/Scripts/Adonis%20Bypass"))()
setthreadidentity(8)

local rem
local EnableOneShot = false
local hook; hook = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
  if EnableOneShot == true and not checkcaller() and typeof(self) == "Instance" and self.ClassName == "RemoteEvent" and self.IsDescendantOf(self, game.ReplicatedStorage.Communication) then
    local args = {...}
    if #args == 3 and (typeof(args[1]) == "Instance" and args[1] == game.Players.LocalPlayer.Character.FindFirstChildOfClass(game.Players.LocalPlayer.Character, "Tool")) and typeof(args[2]) == "Instance" and args[2].IsDescendantOf(args[2], workspace.Monsters) and typeof(args[3]) == "Vector3" then
       for _ = 1, 50 do self.FireServer(self, unpack(args)) end
       rem = self
     elseif #args == 2 and (typeof(args[1]) == "Instance" and args[1] == game.Players.LocalPlayer.Character.FindFirstChildOfClass(game.Players.LocalPlayer.Character, "Tool")) and typeof(args[2]) == "Instance" and args[2].IsDescendantOf(args[2], workspace.Monsters) then
       for _ = 1, 50 do self.FireServer(self, unpack(args)) end
       rem = self
    end
  end
  return hook(self, ...)
end))

local Input = Tabs.Main:AddInput("Input", {
    Title = "Reach weapons",
    Description = "Adjust the range of your weapons",
    Default = "",
    Placeholder = "double",
    Numeric = false,
    Finished = false,
    Callback = function(text)
    for _, tool in next, game.Players.LocalPlayer.Backpack:GetChildren() do
  if tool:IsA("Tool") and tool:FindFirstChild("ReplicatedComponents") then
    for _, part in next, tool:GetDescendants() do
      if part:IsA("BasePart") then
        part.Size = Vector3.new(tonumber(text), tonumber(text), tonumber(text))
        part.CanTouch = false
        part.Massless = true
        Instance.new("SelectionBox", part).Adornee = part
      end
    end
  end
end
end
})

local Toggle = Tabs.Main:AddToggle("MyToggle", {
    Title = "Oneshot weapons", 
    Description = "You can kill any entity with one hit",
    Default = false,
    Callback = function(toggleState)
    EnableOneShot = toggleState
  end
})

local Toggle = Tabs.Main:AddToggle("MyToggle", {
    Title = "Kill aura", 
    Description = "Entities near you die very quickly. First, hit someone with \"Oneshot weapons\"",
    Default = false,
    Callback = function(toggleState)
    euhe = toggleState
    while euhe and task.wait() do
     if game.Players.LocalPlayer.Character:FindFirstChildOfClass("Tool") and rem ~= nil then
      local humanoids = {}
for _, part in next, workspace:GetPartBoundsInRadius(game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").Position, 15) do
    if part.Parent:IsA("Model") and part.Parent:FindFirstChildOfClass("Humanoid") and not part:IsDescendantOf(game.Players.LocalPlayer.Character) then
      if not table.find(humanoids, part.Parent:FindFirstChildOfClass("Humanoid")) then
        table.insert(humanoids, part.Parent:FindFirstChildOfClass("Humanoid"))
      end
    end
  end

      for _, humanoid in next, humanoids do
         rem:FireServer(game.Players.LocalPlayer.Character:FindFirstChildOfClass("Tool"), humanoid.Parent)
      end
    end
   end
  end
})

local Toggle = Tabs.Main:AddToggle("MyToggle", {
    Title = "Collect all coins", 
    Description = "Collect all the coins on the downloaded maps",
    Default = false,
    Callback = function(toggleState)
   dhh = toggleState
     local con = game:GetService("RunService").RenderStepped:Connect(function()
  for _, part in next, workspace.Coins: GetDescendants() do
    if part:IsA("BasePart") and part.Name == "Coin" then
      firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part, 0)
    end
  end
  end)
  task.spawn(function() repeat task.wait() until dhh == false con:Disconnect() end)
  end
})

Tabs.Main:AddButton({
    Title = "Complete all levels",
    Description = "You complete all the levels, BUT there will be very strong lags, so rejoin",
    Callback = function()
    local levellist = {}
for _, txt in next, game.Players.LocalPlayer.PlayerGui.TeleportChooser:GetDescendants() do
  if txt:IsA("TextLabel") then
    table.insert(levellist, txt.Parent.Name)
  end
end

  for _, name in levellist do
    game.ReplicatedStorage.Remotes.NewMap:FireServer("Enter", name)
  end
 end
})

Tabs.Main:AddButton({
    Title = "Search all",
    Description = "Unpack all the lockers",
    Callback = function()
        local pos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
for _, v in next, Workspace.SearchItems:GetDescendants() do
   if v:IsA("ProximityPrompt") then
    local prop = (v.Parent:IsA("BasePart") and "CFrame") or (v.Parent:IsA("Model") and "WorldPivot")
    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = v.Parent[prop]
    fireproximityprompt(v, 0)
  end
end
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = pos
    end
})
